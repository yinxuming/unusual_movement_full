# 股票严重异动监控-实现进度TODO



## 已完成

### 1. 核心功能实现
- [x] 异动规则：10天涨幅100%，30天涨幅200%，适用于全A股（主板、创业板、科创板、北证）
- [x] 参考示例：doc\参考示例\20260608_10天100%30天200%异常波动.jpg
- [x] 同花顺标准数据：doc\参考示例\同花顺严重异动.md（识别导出markdown，作为计算结果对照标准）
- [x] 监控A股股票TOP涨幅接近异动情况
- [x] 异动监控提前量：默认5天可配置（T+0 ~ T+4）
- [x] 异动监控结果展示：股票代码、名称、最新涨幅、异动类型标签、偏离值、是否触发、当日及T+1~T+4触发涨幅限制
- [x] 异动类型二选一：每只股票只显示最接近触发的那种异动（100异动或200异动）
- [x] 按紧急程度排序（当日触发值从小到大，已触发排最后）
- [x] 偏离值列：显示趋势天数和累计涨幅（如"9日偏离值:83.34%"，与同花顺格式对齐）
- [x] 是否触发列：标记"已触发"或"未触发"（红色/灰色标签）
- [x] 偏离值计算修正（与同花顺/交易所官方规则一致）：
  - 偏离值 = 个股区间涨幅 - 对应基准指数区间涨幅
  - 区间涨幅 = (当前收盘价 / 区间最低收盘价 - 1)，取窗口内最低点为基准
  - 基准指数映射：沪市主板→上证A股指数(1.000002)，深市主板→深证A指(0.399107)，创业板→创业板综指(0.399102)，科创板→科创50(1.000688)
  - 触发值公式：x = (1 + threshold + indexGain) / (1 + stockGain) - 1（个股还需涨幅百分比）
  - 获取基准指数K线数据，用于偏离值计算
- [x] 可触发性过滤：只有T+0~T+N中至少一天可触发的才展示
  - T+0日：触发值 <= 当日涨停幅度（主板10%、创业板/科创板20%、北证30%）
  - T+N日：触发值 <= (1+涨停幅度)^(N+1) - 1（连续涨停最大涨幅）
  - 不可触发的T+N值显示为灰色删除线，hover提示"超过N天涨停限制，不可能触发"

### 2. 技术实现
- [x] 前端：HTML + CSS + JavaScript（原生），轻量级无需构建
- [x] 数据获取策略优化：阶段涨幅榜筛选 → 候选股票K线请求（大幅减少请求量）
  - 用clist API获取5日涨幅TOP50 + 当日涨幅TOP50，合并去重
  - 只对候选股票请求K线（通常10-30只，替代原来100只逐个请求）
  - 用K线数据精确计算10日/30日涨幅和异动触发值
- [x] 请求方式：所有请求只走代理（主代理 → 备用代理自动切换），移除直连和模拟数据逻辑
- [x] 代理配置：支持主代理+备用代理自动切换，连续失败自动切换下一个代理
- [x] 代理设置面板：可配置主代理地址、备用代理（每行一个）、代理令牌
- [x] 代理连通性测试：设置面板增加"测试代理"按钮，验证代理是否可用，显示耗时
- [x] 代理配置持久化：保存到localStorage，刷新页面不丢失
- [x] 请求模式显示：页面顶部显示当前请求模式（代理主/备用）
- [x] 智能限流：代理模式并发2-3，避免东方财富限流
- [x] 板块涨停幅度识别：主板10%、创业板/科创板20%、北证30%
- [x] 识别结果缓存：localStorage缓存，当日有效（跨天失效），点击刷新清空缓存强制刷新
- [x] K线数据localStorage缓存：当日有效，避免重复请求
- [x] 自动刷新、手动刷新、设置面板（监控数量、提前天数、自动刷新间隔）
- [x] 表格排序（点击表头切换排序）
- [x] 交易日历集成：基于chinese-days节假日数据，判断A股交易日
  - CDN加载节假日数据（cdn.jsdelivr.net/npm/chinese-days@1），localStorage缓存
  - 判断当前是否交易时间（9:30-11:30, 13:00-15:00）
  - 收盘后自动偏移：T+0=下一交易日，触发值重新计算
  - 表头T+N列显示实际交易日日期（如6/15而非T+0）
  - 数据日期显示"K线日期: 2026-06-12 → 预测: 2026-06-15"
- [x] 默认不自动刷新（autoRefresh=0），用户可手动开启

### 3. 部署支持
- [x] GitHub Pages部署：.nojekyll文件，纯前端运行
- [x] 本地开发：python server.py 启动静态文件服务器（端口复用、局域网IP访问）

## 待优化
1. [x] 计算结果与同花顺标准数据对比验证，确保偏离值和触发条件一致
   - 已修正：偏离值 = 个股涨幅 - 指数涨幅（与交易所官方公式一致）
   - 已修正：触发值公式考虑指数涨幅影响
   - 已修正：基准指数映射（沪市→上证A股，深市→深证A指，创业板→创业板综指，科创板→科创50）
   - 验证案例：金安国纪6.15触发值9.24%（与同花顺一致），旧值22.4%已修正
2. [x] **修正tradeDayOffset滑动窗口逻辑（对齐同花顺标准）** ← 2026-06-13
   - 问题现象：金安国纪从结果中消失，6.15预测触发值10.49% vs 同花顺9.24%（误差13.4%）
   - 根因分析：旧逻辑在tradeDayOffset>0时滑动窗口+截取triggers数组，导致T+0触发值偏大
   - 同花顺规则（基于20260608截图和标准数据验证）：收盘后预测下一交易日时，T+0（目标日当天）**不滑动窗口**，直接用当前最新K线窗口计算；T+1才开始考虑滑动窗口效应
   - 修正方案：移除analyzeStock中tradeDayOffset时的窗口滑动和triggers截取逻辑，tradeDayOffset仅作为显示偏移
   - 修正后结果：金安国纪6.15预测 T+0=9.24%（=6.12当天），偏离值83.34%，hasAchievableRisk=true，不再被过滤
   - 测试验证：test_node.js 6/6通过，test_actual.js 4/4通过
3. [x] **v15: 增加异动限制列** - 当前幅度后新增"异动限制"列显示板块涨停幅度(±10%/±20%/±30%)
4. [x] **v15: 排查缺少股票+放宽候选筛选** ← 2026-06-13
   - 索辰科技(688507): 已在结果中，触发值差异待调查
   - 莱伯泰科(680056): 因当日涨幅+3.13%低于旧阈值8%被过滤
   - 修正: 5日涨幅阈值20%→15%, 当日涨幅阈值8%→3%
5. [x] **v15: 加强可触发性过滤+默认4天** ← 2026-06-13
   - 过滤逻辑: 只检查T+0和T+1可触发性(原T+0~T+N任意一天)
   - 原因: T+2及以后需连续多天涨停才可能触发,实际概率极低
   - forwardDays: 5→4 (T+0~T+3)
6. [x] **v15: 板块颜色区分+代码超链接** ← 2026-06-13
   - 创业板(30xxxx): 天蓝色#38bdf8 | 科创板(68xxxx): 紫色#c084fc | 北证(8/4xxx): 橙色#fb923c
   - 股票代码链接到东方财富quote.eastmoney.com详情页
7. [x] **v18: 异动统计+颜色+滑动窗口修正** ← 2026-06-14
   1. 异动统计修正：统计逻辑从只看T+0改为检查所有T+N天是否有可触发的，与同花顺6.15有7条异动风险一致
   2. 触发值颜色修正：阈值从硬编码5%/10%改为基于涨停幅度（danger<50%涨停, warning<涨停），科创板15.12%触发值正确标黄
   3. T+1不滑动窗口：同花顺6.15触发值=T+0额外涨幅（金安国纪9.24%），不使用calcDayTrigger(dayOffset=1)的滑动窗口结果
   4. T+2及以后使用滑动窗口：6.16及以后日期的触发值会随窗口滑动而变化，不再全部相同
   5. 测试用例9项全部PASS：金安国纪6.12/6.15偏离值、触发值、可触发性均与同花顺一致
8.  [ ] 支持导入同花顺实时数据作为筛选源（替代clist API筛选）
9.  [ ] 收盘后候选股票补充：窗口滑动后可能新增进入监控的股票（如金安国纪6.12不在监控，6.15窗口滑动后进入），需考虑扩大筛选范围或增加T+1预计算筛选

## 2026-06-15 更新

### isTriggerAchievable逻辑修正
- **问题**：6.12统计7条异动风险，同花顺标准5条，多出金安国纪和索辰科技
- **根因**：isTriggerAchievable对T+N日判断使用累计涨停涨幅（如T+1最大21%），但触发值表示"当日需涨多少"，单日最多涨涨停幅度
- **修正**：isTriggerAchievable统一判断触发值<=单日涨停幅度，不再使用N天累计涨幅
- **影响**：金安国纪6.12（100异动T+1=10.97%>10%主板涨停）不再被统计，与同花顺一致

### 测试验证结果（test_ths_verify.js）
- 25项测试，24项通过，1项差异在3%误差范围内
- 唯一差异：有研粉材6.12趋势天数（实际9，同花顺10），偏离值差2.57%，触发值差1.25%
- 原因分析：同花顺可能使用了不同的窗口起点选择算法，导致基准点早1天
- 6.15统计：5条可触发（缺雷信科技和莱伯泰科K线数据，加上应为7条，与同花顺一致）
- 6.12统计：7条（同花顺5-6条，差异主要在金安国纪和索辰科技的筛选标准）

### dominantRule选择逻辑优化
- 优先选择有可触发的规则中触发值最小的
- 如果没有可触发的规则，选触发值最小的
- hasAchievableRisk只检查T+0和T+1（与同花顺一致）


## 2026-08-30 更新：v19 自选股功能+左侧菜单改版

### 新增功能（详见 doc/自选股功能与左侧菜单改版.md）
1. [x] 首页改版为左侧菜单栏三页布局：市场行情/自选/设置，tab状态持久化（unusual_active_tab）
2. [x] 自选模块（js/watchlist.js新增）：localStorage CRUD、CSV导入（GBK/UTF-8自动识别、多代码格式兼容）、CSV导出（UTF-8 BOM）、全A股搜索添加
3. [x] 自选二级菜单：异动风险（默认，复用异动计算管线）/ 普通浏览（行情快照+阶段涨幅+涨停信息27列宽表，二级菜单状态持久化 unusual_wl_subtab）
4. [x] renderer.js工厂化改造：createTableRenderer支持多实例（市场行情/自选互不干扰），默认实例兼容旧接口
5. [x] api.js新增：getQuoteBatch批量行情/getZTPool涨停池/getTHSZTReason同花顺涨停原因/getAllStockList全量列表/clearKlineCache；batchGetKline支持limit参数（浏览视图260根K线）
6. [x] 设置弹窗迁入独立设置页（main.js移除旧弹窗逻辑）

### 测试
- test_case/test_csv_parse.js：35项全部PASS（含真实GBK CSV文件导入解析）
- 浏览器验证7项全部通过（页面渲染/接口请求响应/tab记忆）

### 已知问题
- 备用代理腾讯云函数DNS解析失败，自动切换主代理，不影响功能（可在设置页更换）
